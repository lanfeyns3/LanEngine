#pragma once

#include <stdint.h>
#include <string>

namespace LANE
{
    namespace Components
    {
        struct Info
        {
            uint64_t uuid;
            std::string name;
        };

        struct Renderer // TODO: better name
        {

        };
    } // namespace Components
    
} // namespace LANE
